# Optional: No environment variables required for basic operation.
# API keys are provided via the web UI for each export.
#
# APP_API_KEY - If set, all /export requests must include X-API-Key header with this value.
# CORS_ORIGINS - Comma-separated list of allowed origins (e.g. https://app.example.com). Empty = same-origin only.
